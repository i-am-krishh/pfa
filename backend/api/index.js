// Vercel Serverless Function entry point for backend-only deployment
// Vercel Root Directory is set to "backend", so this file lives at backend/api/index.js
import app from '../server.js';

export default app;
